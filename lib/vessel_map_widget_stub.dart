import 'package:flutter/material.dart';

class VesselMapWeb extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Tidak tersedia di platform ini')),
    );
  }
}
